{
  "name": "@dev-arora/tizenflix",
  "version": "0.1.1",
  "description": "TizenBrew streaming client for Tizenflix (educational purposes only)",
  "packageType": "app",
  "appName": "Tizenflix",
  "appPath": "app/index.html",
  "keys": [
    "MediaPlayPause",
    "MediaFastForward",
    "MediaRewind"
  ],
  "license": "MIT",
  "files": [
    "app",
    "package.json",
    "README.md"
  ],
  "scripts": {
    "build": "esbuild app/js/main.js --bundle --outfile=app/dist/app.bundle.js --target=es2015 --format=iife --global-name=TizenflixApp",
    "start": "npm run build && node scripts/dev.mjs",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
